-- Комментарий поставщика вместо контактных данных.
--
-- Зачем. На публичной странице /uploads поля «Организация или ФИО» и «Телефон
-- для связи» оказались лишним трением: поставщик и так известен по документам,
-- которые он прикладывает, а хранение телефона — это персональные данные без
-- реальной пользы. Вместо них — свободный комментарий к поставке («две машины,
-- вторая после обеда»), который менеджер видит в карточке документа.
--
-- Удаление колонок безопасно: публичный вход ещё ни разу не работал на проде
-- (был выключен), в ingest_events нет ни одной строки с channel='public' —
-- проверено запросом перед миграцией.
--
-- submitter_ip и submitter_user_agent ОСТАЮТСЯ: это техническая запись на
-- случай разбора злоупотреблений, в интерфейс она не выводится.

ALTER TABLE "ingest_events" ADD COLUMN IF NOT EXISTS "submission_comment" text;
--> statement-breakpoint
ALTER TABLE "ingest_events"
  DROP COLUMN IF EXISTS "submitter_name",
  DROP COLUMN IF EXISTS "submitter_phone";
