-- Откат 0116.
--
-- ДЕСТРУКТИВНО в узком смысле: повторный разбор перестанет знать, каким
-- промптом создавался документ, и для накладных формы 1-Т снова пойдёт
-- активным промптом ТН-2116 — то есть вернёт результат не по тем колонкам.
-- Сами документы не затрагиваются.

ALTER TABLE "source_documents"
  DROP COLUMN IF EXISTS "waybill_prompt_kind";
