-- Личный кабинет: пользователь может указать ФИО (Иванов Иван Иванович).
-- Поле опциональное (nullable), чтобы не ломать существующих users без ФИО.
-- Новые пользователи могут добавить через регистрацию или позже через ЛК.
-- В админ-таблице «Пользователи» отображается отдельной колонкой.

ALTER TABLE "users"
  ADD COLUMN IF NOT EXISTS "full_name" text;
