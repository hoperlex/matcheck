-- Откат 0106.
--
-- ДЕСТРУКТИВНО в одном узком смысле: вместе с колонкой пропадает возможность
-- сопоставить файл повторной отправки с недозагруженной строкой реестра по
-- содержимому. Пакеты, у которых часть файлов не залилась в S3, после отката
-- придётся дозагружать вручную — код вернётся к сопоставлению по имени и
-- порядку, а оно неоднозначно.
--
-- Сами файлы и строки реестра не затрагиваются.

ALTER TABLE "bundle_import_items"
  DROP COLUMN IF EXISTS "content_sha256";
