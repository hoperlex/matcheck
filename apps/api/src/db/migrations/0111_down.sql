-- Откат 0111: НЕ ПРЕДУСМОТРЕН, файл намеренно ничего не делает.
--
-- 0111 — адресная правка данных, а не схемы: она закрыла строку реестра,
-- повисшую после удаления документа. Обратный UPDATE (resolved_at = NULL)
-- не вернул бы прежнее состояние, а воспроизвёл бы сам дефект: файл снова
-- всплыл бы в списке строкой «в очереди», и вдобавок потерялась бы записанная
-- причина закрытия.
--
-- Если строку понадобится открыть заново (например, файл решили разобрать
-- руками), это осознанное действие оператора по конкретной строке, а не откат
-- миграции.
SELECT 1;
