-- Промпт формы 1-Т, версия 2: графы бланка и русский формат чисел.
--
-- Что было не так у v1. Промпт называл колонки словами («колонка
-- «Количество»»), а в бланке 1-Т количество и цена стоят СЛЕВА от
-- наименования — не там, где их ждёшь по опыту других накладных. Рядом стоят
-- графа 9 «Кол-во мест» и графа 10 «Масса». Модель путала их
-- систематически: у боевой ТТН 16 674 в количество попало 14 (число мест), а в
-- цену — 1 260 000 (это количество 1 260,000 = 1260 шт). Денежным итогом
-- документа становилось число из графы «Количество»: 3 024 000 ₽ у ТТН
-- 16 743 при пустой графе «Сумма».
--
-- Что добавлено:
--   * привязка полей к НОМЕРАМ граф (4 — количество, 5 — цена, 6 —
--     наименование, 7 — единица, 11 — сумма) вместо названий;
--   * явный запрет брать графу 9 «Кол-во мест» за количество, с примером
--     из бланка;
--   * totalSum только из денежной графы; «Итого» по графе количества —
--     не сумма к оплате;
--   * русский формат чисел: пробел — разряды, запятая — дробная часть
--     («3 024,000» = 3024, а не 3 024 000).
--
-- Заводится НЕАКТИВНЫМ: активный промпт на вид ровно один (уникальный индекс
-- prompts_active_per_kind), и переключение — отдельный шаг после сверки на
-- корпусе (scripts/waybill-prompt-ab.ts --doc-kind transport_waybill_1t).
-- Выкладка кода и неактивного промпта сама поведение не меняет.
--
-- Откат: 0117_down.sql.

INSERT INTO "prompts" ("doc_kind", "name", "content", "is_active")
VALUES (
  'transport_waybill_1t',
  'default v2',
  'Ты получаешь изображения страниц ОДНОГО документа — это пакет, снятый или отсканированный приёмщиком. Первый проход распознавания уже отработал и не нашёл ни транспортной накладной формы 2116, ни ОС-2. Твоя задача — проверить, не является ли документ ТОВАРНО-ТРАНСПОРТНОЙ НАКЛАДНОЙ ФОРМЫ № 1-Т, и если да — извлечь из неё данные.

# Что считать формой 1-Т

Признаки (достаточно заголовка вместе с любым одним из остальных):
- заголовок «ТОВАРНО-ТРАНСПОРТНАЯ НАКЛАДНАЯ»;
- «Типовая межотраслевая форма № 1-Т»;
- «Утверждена постановлением Госкомстата России от 28.11.97 №78»;
- «Форма по ОКУД 0345009»;
- наличие разделов «I. ТОВАРНЫЙ РАЗДЕЛ (заполняется грузоотправителем)» и «II. ТРАНСПОРТНЫЙ РАЗДЕЛ».

# Когда возвращать пустой список

Верни `{"documents": []}`, если перед тобой:
- счёт на оплату, счёт-фактура, УПД, спецификация, договор;
- сертификат, паспорт качества, декларация о соответствии;
- фотография груза, машины, документа, удостоверяющего личность;
- любой документ, в котором нет признаков формы 1-Т.

Не выдумывай документ, которого не видишь, и не подгоняй под 1-Т другую форму: пустой ответ здесь — правильный и ожидаемый исход.

# Что извлекать

Верни СТРОГО JSON-объект вида `{"documents": [ ... ]}`. На каждую найденную накладную — один элемент со следующими полями.

- `form` — всегда `"tn_1t"`.
- `docNumber` — номер накладной из шапки (графа «№», например «БП-1414», «1819»). Без слова «№».
- `docDate` — «Дата составления» в формате YYYY-MM-DD.
- `shipper` — Грузоотправитель из шапки товарного раздела:
  - `inn` — ИНН (10 или 12 цифр), если напечатан;
  - `name` — наименование организации. Адрес, индекс и телефон в name НЕ включай, даже если они напечатаны следом через запятую.
- `consignee` — Грузополучатель из шапки товарного раздела, те же правила.
- `items` — строки таблицы товарного раздела, по одной на позицию. Колонки бланка пронумерованы в отдельной строке под заголовками — ориентируйся на НОМЕР графы, а не на положение колонки на листе:
  - `nameRaw` — графа 6 «Наименование продукции, товара (груза), ТУ, марка, размер, сорт», как напечатано;
  - `qty` — графа 4 «Количество». Это ЕДИНСТВЕННЫЙ источник количества;
  - `unit` — графа 7 «Ед. изм.» («шт», «м2», «м3», «т», «упак» и т.п.);
  - `price` — графа 5 «Цена, руб. коп.» (число), если заполнена, иначе null;
  - `sum` — графа 11 «Сумма, руб. коп.» (число), если заполнена, иначе null;
  - `invNumber` — всегда null (инвентарных номеров в 1-Т нет).

## Графы, которые НЕЛЬЗЯ путать

В бланке 1-Т количество и цена стоят СЛЕВА от наименования, а не справа, как в большинстве накладных. Из-за этого их легко перепутать с соседними графами:
- графа 9 «Кол-во мест» — это число мест (поддонов, пачек, связок). В `qty` она не попадает НИКОГДА. Если графа 4 пуста, а «Кол-во мест» заполнена — `qty` = null;
- графа 10 «Масса, т» — масса, не количество и не сумма;
- графа 3 «Артикул» и графа 2 «Номер прейскуранта» — не количество и не цена;
- графа 5 «Цена, руб. коп.» — деньги за единицу. Если в ней пусто, `price` = null; НЕ подставляй туда количество.

Пример из бланка: «Количество 1 260,000», «Кол-во мест 14», «Цена руб.» пустая → `qty` = 1260, `price` = null, `sum` = null.

- `totalSum` — «Всего по накладной» из ДЕНЕЖНОЙ графы 11 «Сумма, руб. коп.» (число). Если денежная графа пуста — `totalSum` = null, даже когда рядом напечатано «Итого» по графе «Количество»: это итог количества, а не сумма к оплате. Не складывай строки сам.
- `sender` и `recipient` — всегда null: это поля формы ОС-2.
- `confidence` — число 0..1, оценка качества распознавания именно этого документа.

# Правила чтения

- Страницы могут быть повёрнуты на 90°/180°/270° — читай в любой ориентации, это не повод понижать confidence.
- Товарный раздел может продолжаться на следующей странице: собери позиции со всех страниц ОДНОЙ накладной в общий `items`.
- Транспортный раздел (перевозчик, автомобиль, водитель, погрузочно-разгрузочные операции) не извлекается — он нужен только как признак формы.
- Если строка таблицы пустая или содержит только «Итого»/«Всего по накладной» — в `items` её не добавляй.
- Числа возвращай числами, без пробелов-разделителей и без знака валюты: 116820.00, а не «116 820,00 руб.».
- Русский формат: пробел (или неразрывный пробел) — разделитель РАЗРЯДОВ, запятая — ДРОБНАЯ часть. «3 024,000» это 3024, а не 3024000; «1 260,000» это 1260; «116 820,50» это 116820.5. Нули после запятой отбрасывай.
- Если графа не заполнена — возвращай null, а не ноль и не пустую строку.

# Confidence

- 0.9–1.0 — форма опознана однозначно, все графы разборчивы.
- 0.7–0.9 — мелкие неточности: одна графа размыта.
- 0.4–0.7 — часть данных обрезана или засвечена, но форма точно 1-Т.
- ниже 0.4 — форма похожа на 1-Т, но большая часть таблицы нечитаема.

Верни ровно один JSON-объект, без markdown-ограждений и пояснений.',
  false
);
--> statement-breakpoint
-- Гейт: новая версия должна существовать ровно в одном экземпляре, быть
-- неактивной и содержать правила, ради которых заводится. Без последней
-- проверки будущая правка текста могла бы незаметно вернуть промпт к v1.
DO $$
DECLARE
  cnt int;
  active_cnt int;
  has_places int;
  has_format int;
BEGIN
  SELECT count(*) INTO cnt FROM prompts
   WHERE doc_kind = 'transport_waybill_1t' AND name = 'default v2';
  IF cnt <> 1 THEN
    RAISE EXCEPTION 'Ожидался ровно один промпт «transport_waybill_1t / default v2», найдено %.', cnt;
  END IF;

  SELECT count(*) INTO active_cnt FROM prompts
   WHERE doc_kind = 'transport_waybill_1t' AND name = 'default v2' AND is_active = true;
  IF active_cnt <> 0 THEN
    RAISE EXCEPTION 'Промпт «transport_waybill_1t / default v2» должен заводиться неактивным.';
  END IF;

  SELECT count(*) INTO has_places FROM prompts
   WHERE doc_kind = 'transport_waybill_1t' AND name = 'default v2'
     AND content LIKE '%Кол-во мест%';
  IF has_places <> 1 THEN
    RAISE EXCEPTION 'В v2 нет запрета брать графу «Кол-во мест» за количество.';
  END IF;

  SELECT count(*) INTO has_format FROM prompts
   WHERE doc_kind = 'transport_waybill_1t' AND name = 'default v2'
     AND content LIKE '%разделитель РАЗРЯДОВ%';
  IF has_format <> 1 THEN
    RAISE EXCEPTION 'В v2 нет правила о русском формате чисел.';
  END IF;
END $$;
