-- Откат 0115.
--
-- ДЕСТРУКТИВНО в узком смысле: вместе с колонкой пропадает признак, по которому
-- склейка отличает два экземпляра одной УПД от двух частей одного документа, а
-- валидатор видит задвоенные строки. Логика вернётся к сопоставлению строк по
-- количеству и сумме — оно работает, но слабее.
--
-- Сами позиции документов не затрагиваются.

ALTER TABLE "source_document_items"
  DROP COLUMN IF EXISTS "row_no";
