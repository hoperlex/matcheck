-- Транзит — флаг «приёмка/отгрузка является частью транзитного рейса»
-- (машина приехала/отгрузила, но дальше едет с другим грузом).
-- Заполняет инспектор чекбоксом на 1 этапе мобилы: для picked-UPD случая
-- справа от «Тип транспорта», для empty-draft — ниже комментария.
--
-- На веб-портале — небольшой тег «🚚 Транзит» в шапке карточки рядом с
-- Объект/Получатель/Госномер/УПД/Сумма (Приёмка) или
-- Откуда/Получатель/Госномер/Водитель/Тип отгрузки (Отгрузка). Тег
-- рисуется ТОЛЬКО когда in_transit = true.
--
-- NOT NULL DEFAULT false — legacy-записи получают false, ничего не
-- ломается. Поле боолевое, не enum — список значений семантически
-- бинарный.

ALTER TABLE "deliveries"
  ADD COLUMN "in_transit" boolean NOT NULL DEFAULT false;

ALTER TABLE "shipments"
  ADD COLUMN "in_transit" boolean NOT NULL DEFAULT false;
