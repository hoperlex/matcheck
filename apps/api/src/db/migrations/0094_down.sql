-- Откат 0094: убираем колонку «Проверять» (review) из матрицы прав.
--
-- Что теряется. Настройки этого действия — безвозвратно (колонка удаляется
-- вместе со значениями). Прочие галочки не затрагиваются.
--
-- Когда нужен. Только вместе с откатом кода на версию БЕЗ действия review: если
-- вернуть старый код, но оставить колонку, ничего не сломается — старый резолвер
-- о ней не знает и не читает. Обратный порядок опаснее: новый код без колонки
-- уронит SELECT матрицы. Поэтому откатывать SQL нужно ПОСЛЕ отката кода, а не
-- до него.
--
-- Важно: старый код трактует отметку проверки как часть operations.*:edit. Если
-- к моменту отката админ успел выдать монитору настоящий edit, после возврата
-- старой версии это право снова схлопнется в «только отметка» — allow-list
-- маршрутов flags/link-source/share-link там по-прежнему manager-only.
--
-- В meta/_journal.json НЕ регистрируется. Применять вручную:
--   psql "$DATABASE_URL" -1 -f apps/api/src/db/migrations/0094_down.sql
--   DELETE FROM drizzle.__drizzle_migrations WHERE hash = '<sha256 файла 0094>';

ALTER TABLE "role_page_permissions" DROP COLUMN IF EXISTS "can_review";
