import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    // Файлы выполняются ПОСЛЕДОВАТЕЛЬНО, а не параллельно.
    //
    // Интеграционные наборы делят одну тестовую базу, и часть из них работает
    // с общими таблицами: job_outbox чистится целиком в job-outbox.int, а
    // строки в неё пишут ещё и router-provenance.int, mail-resolve.int и
    // публичная загрузка. При параллельном запуске один набор стирал строки
    // другого прямо посреди проверки — тесты «зелёные поодиночке, красные в
    // наборе», причём каждый раз разные. Комментарий об этой опасности уже
    // стоял в router-provenance.int.test.ts.
    //
    // Плата — полный прогон идёт ~45 с вместо ~13 с. Нестабильный набор стоит
    // дороже: ему перестают верить.
    //
    // Убрать это можно только вместе с изоляцией БД (схема или база на файл).
    fileParallelism: false,
  },
});
